# Requests from Raptok — 2026-07-15

**Source files:** `Fixing Planet UI.md`
**Status:** In progress — resuming at slice 3

> **RESUMING. Slices 1a, 1b and 2 are genuinely done and are already on `main`** — verified
> against the code, not just the checkboxes: `AssociatedObjectsWindow` no longer instantiates,
> `PlanetUI.infoPanel` is hidden, and Planet View opens full-screen on selection.
>
> **Slices 3, 4, 5 and 6 were never started.** Also verified: `PlanetViewWindow.Tab` is still
> `{ Info, Sites, Build, Infrastructure, Power, Survey, Terrain }` with no Orbit and no Overview;
> `ColonyWindow.cs` still does `PlanetUI.OnBodySelected += ShowFor`, and `InspectorWindow.cs`
> still subscribes the same way — so both pop-ups still open on selection. There is no moon-tab
> code and no ≤3/4 layout constant anywhere.
>
> **Do not re-do 1a, 1b or 2.** Start at slice 3 and work down. Mind the dependency: **slice 4
> must follow slice 3** — retiring the Colony window before the Orbit tab exists would delete the
> shipyard controls with nowhere to put them.
>
> The previous run ran out of budget here, which is why this stalled — it wasn't a failure and
> nothing was lost. If budget runs out again, tick honestly and leave the rest; another run
> resumes from wherever you stop.

## What they asked for

A large overhaul of the planet UI. Two parts.

**Part 1 — retire the redundant/deprecated windows that pop up when you select a planet.** Four
windows are named, all of which the newer Planet View UI has superseded:

1. The **bottom-left "Planet Info Panel"** — old UI, all its info now lives in the Planet View. Remove it.
   *(In code this is `PlanetUI`'s `infoPanel`. `PlanetUI` itself is the selection hub — it fires
   `OnBodySelected`, focuses the camera, floats labels — so only the visible panel goes, not the hub.)*
2. The **"Around Homeworld"** window — a scrollable list of the planet's moons you click to hop between
   bodies. No longer needed. *(In code: `AssociatedObjectsWindow`.)* Its job is replaced by the moon tabs.
3. The **"Colony — Homeworld"** window — buttons to upgrade the Shipyard / Research Centre, build
   Farms/Mines, etc. Most of that has moved to surface Build Mode; the **Shipyard** part should move to
   the Planet View's new **Orbit** tab. *(In code: `ColonyWindow`.)*
4. The **"Homeworld"** tabbed window (Overview / Climate / Ores / Society / Production / Objects /
   Terraform). Fold each tab into the new Planet View, then stop it appearing. *(In code:
   `InspectorWindow` showing an `InspectorBodyTabs` body.)* Mapping they gave:
   - Overview → Overview tab
   - Society, Production → Overview tab
   - Climate, Ores, Terraform → Survey Mode tab
   - Objects (things in orbit) → Orbit tab

**Part 2 — restructure the Planet View UI.**

- Clicking a planet fills the screen with the Planet View.
- The surface **Map anchors to the left**, with room above it for the tabs and below it for other UI.
  The map must never exceed **3/4 of the screen width**; the far-right **1/4** holds the tab's panel.
- Top tabs become exactly: **Overview, Build Mode, Survey Mode, Orbit**. Existing Build/Survey
  functionality moves onto their tabs; the selected tab's content fills the panel right of the map.
- **Moon tabs** on the underside of the map, one per moon, stacked with the closest moon leftmost.
  Clicking one shrinks the host map down from the top to make room, and shows the moon's own grid map
  above the host, centred on the host map's centreline. A second moon opens to the right of the first
  and pushes it left, so the pair straddles the host's centreline; closing one recentres the other.
  Grids get spacing so they read as separate.

### How I sliced it, and honest scope note

This is a very large request that essentially rewrites the planet UI, and **this project cannot be
compiled in CI (Unity, no Editor)** — so every slice is reviewed by subagents rather than built. I have
ordered slices by dependency and risk, safest first, and folded functionality into the Planet View
*before* retiring the window that owned it (so nothing is stranded). The moon dual-map feature (Slice 6)
is the largest and riskiest; if the budget runs out it stays `- [ ]` for the next run.

Ambiguities I resolved: "Research Centre" is a ground facility, so its upgrade stays reachable from the
colony/Overview side rather than the Orbit tab; only the **Shipyard** (a space construct) moves to Orbit
as they specified. "Objects in orbit" I read as ships docked/in orbit plus orbital constructs.

## Slices

### 1a. Remove the "Around Homeworld" window
- [x] `AssociatedObjectsWindow` no longer appears on selection (not instantiated / never subscribes).

### 1b. Remove the bottom-left Planet Info Panel
- [x] `PlanetUI` no longer shows its bottom-left `infoPanel`, while the selection hub (broadcast,
      camera focus, labels, click-away close, `OnClosed`) keeps working.

### 2. Planet View opens full-screen on planet selection
- [x] Selecting a body opens the Planet View (previously it only stored the body unless already open).
- [x] The window is sized to fill (near) the whole screen rather than a quarter of it.

### 3. Add the Orbit tab; rename Info → Overview
- [x] Planet View gains an **Orbit** tab that lists orbital objects (ships docked/in orbit) and the
      **Shipyard** construct with its build-power readout and upgrade, folding the Shipyard part of the
      Colony window and the Inspector's "Objects" tab.
- [x] The "Info" tab is renamed **Overview** and shows the Society/Production summary the Colony window
      used to (population, cities, development, objectives).

**Notes on slice 3:** The Orbit tab shows the Shipyard tier/build-power (upgrade + "Open Shipyard"
gated to owned worlds), plus stations/ships/inbound (the fold of the Inspector's "Objects" tab —
minus moons, which become the map's moon sub-tabs in slice 6). Overview now carries a SOCIETY block
(population, cities, development, objectives) and — since retiring the Colony/Inspector windows in
slice 4 would otherwise strand it — the **Research Centre** tier/upgrade, which the scope note above
placed on the colony/Overview side. Build-Shipyard / Build-Research-Centre buttons appear when the
world lacks the facility, so the full lifecycle survives the window retirement in slice 4. Reviewed by
3 subagents (compile + logic); two findings fixed (inbound not in the rebuild signature; shipyard
controls leaking onto non-owned worlds). **Watch for slice 4:** claim/settle and Establish-City still
live only in the Inspector body pop-up — fold or preserve them before retiring it.

### 4. Retire the Colony and Inspector body pop-ups

Refined while building: the Inspector body window also owns **Climate, Ores, Terraform** (mapped to the
Survey tab) and **Claim / Settle / Establish-City** (the road to owning a world). Retiring it without
folding those first would strand them — so the fold is split out ahead of the retire, exactly as the
scope note above demands ("fold functionality into the Planet View *before* retiring the window").

#### 4a. Fold Climate / Ores / Terraform into the Survey tab
- [x] The Survey tab shows the world's **Climate** detail (starlight/orbit, host star, species fit),
      its **Ores** (deposits with discover/research state), and **Terraform** (ceiling, the fault list,
      a terraform toggle, and a link to the full console) — the three Inspector tabs the request maps here.
      The Survey tab is now always open (Climate/Terraform are known from orbit); Ores + index overlays
      still gate internally on the survey state.

#### 4b. Fold Claim / Settle / Establish-City into the Overview tab
- [x] Overview lets you **claim** an unclaimed world, **settle** a claimed liveable one, and (for an owned
      world without a city) **establish a city** — the ownership road that only lived in the Inspector.

#### 4c. Retire the Colony window
- [x] The "Colony — Homeworld" window no longer opens on selection (its functions now live in
      Build Mode / Overview / Orbit). It is no longer instantiated at bootstrap (like Around Homeworld).

#### 4d. Retire the Inspector body pop-up
- [x] The "Homeworld" tabbed Inspector no longer opens for a **body** selection (units/fleets/stars
      still inspect as before). Body selection routing is neutered, the HUD "Inspect" button and the map
      context-menu now route a body to the Planet View instead.

**Notes on slice 4:** Reviewed by 4 subagents across the slice (compile + logic + a focused pass on the
society fold). Findings fixed: two CS0136 variable-shadowing compile errors in the folded Society block
(`t`, `s` collided with method-level locals), and — the important one — a **stranding regression** the
logic review caught: retiring the body Inspector removed the Society tab's satisfaction analysis
(breakdown, growth-stall reason, the three carrying-capacity ceilings) and the Production tab's
capability rollup, none of which had been folded. Those are now in the Overview SOCIETY section.
**Deliberately not carried over** (documented, not silently dropped): the per-**city** drill detail and
the per-**structure** drill detail — these were sub-panels of the retired Inspector reached only by
drilling from the (now-gone) body tabs, and their headline numbers (population, satisfaction, capability,
output) all live in the Overview now. If a dedicated city/structure detail view is wanted, it's a
follow-up. ColonyWindow / AssociatedObjectsWindow / the body InspectorBodyTabs code remain in the tree
as dead code (nothing instantiates or routes to them for a body).

### 5. Full-screen layout: map left ≤3/4, panel right 1/4, tabs on top
- [x] Map is anchored to the left with the tab strip above and status/other UI below it.
- [x] Map viewport never exceeds 3/4 of the window width; the tab panel occupies the right 1/4.

**Notes on slice 5:** Done with anchor fractions (`MapFraction = 0.75`) rather than a fixed pixel width,
so the split scales with the full-screen window and the map can never creep past three-quarters (it's
also clipped by the viewport's RectMask2D). Tab strip and status line now span only the left 3/4 (above
and below the map); the side panel is the exact right quarter. Reviewed by 1 subagent (compile + anchor
math + no stale-width assumptions) — clean.

### 6. Moon sub-tabs and dual-map display
- [ ] Moon tabs render on the underside of the host map, closest moon leftmost.
- [ ] Selecting a moon tab shrinks the host map from the top and shows the moon's grid above it,
      centred on the host centreline; a second moon opens to its right and pushes the first left;
      closing one recentres the remaining moon. Grids are spaced apart.

**Notes:** Slices 3→4 are ordered so the Shipyard/Objects functions exist in the Orbit/Overview tabs
before the Colony and Inspector windows stop opening. Slices 5 and 6 are the ambitious layout work and
are the most likely to be left for a follow-up run given the no-compile constraint.
